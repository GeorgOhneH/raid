[package]
name = "raid-fuzz"
version = "0.0.0"
authors = ["Automatically generated"]
publish = false
edition = "2021"

[dependencies]
raid = {path = ".."}
rand = "0.8"

# Prevent this from interfering with workspaces
[workspace]
members = ["."]